import { Easing, interpolate, useCurrentFrame } from "remotion";
import { FadeUp } from "../components/FadeUp";
import { SceneFrame } from "../components/SceneFrame";
import { palette } from "../constants";

const values = [["01", "DataHub-grounded", "Fields and policies come from the enterprise catalog"], ["02", "Local generation", "The model uses an internal loopback endpoint"], ["03", "Runtime proof", "Java and Rust safe outputs mask real values"], ["04", "Fail closed", "Missing context stops generation"]];

export const ClosingScene: React.FC = () => {
  const frame = useCurrentFrame();
  return <SceneFrame chapter="CLOSE" step="07 / 07"><div style={{ height: "100%", display: "flex", flexDirection: "column", justifyContent: "center" }}><FadeUp name="Closing title" delay={5}><div style={{ color: palette.cyan, fontFamily: "monospace", fontSize: 18, fontWeight: 800, letterSpacing: 3 }}>FROM GOVERNANCE TO RUNNABLE CODE</div><h2 style={{ margin: "16px 0 0", fontSize: 88, letterSpacing: -4.5 }}>From governance to runnable code</h2><p style={{ margin: "22px 0 0", color: palette.muted, fontSize: 30 }}>Traceable, reproducible, secure by default, and designed for enterprise Coding Agent workflows.</p></FadeUp><div style={{ marginTop: 58, display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: 18 }}>{values.map(([number, title, detail], index) => <div key={number} style={{ minHeight: 230, padding: 26, border: `1px solid ${palette.line}`, borderRadius: 18, backgroundColor: "rgba(13,27,31,0.9)", opacity: interpolate(frame, [42 + index * 12, 64 + index * 12], [0, 1], { extrapolateLeft: "clamp", extrapolateRight: "clamp", easing: Easing.bezier(0.16, 1, 0.3, 1) }), transform: `translateY(${interpolate(frame, [42 + index * 12, 64 + index * 12], [30, 0], { extrapolateLeft: "clamp", extrapolateRight: "clamp", easing: Easing.bezier(0.16, 1, 0.3, 1) })}px)` }}><div style={{ color: palette.cyan, fontFamily: "monospace", fontSize: 17, fontWeight: 850 }}>{number}</div><div style={{ marginTop: 26, fontSize: 27, fontWeight: 820 }}>{title}</div><div style={{ marginTop: 13, color: palette.muted, fontSize: 18, lineHeight: 1.5 }}>{detail}</div></div>)}</div><FadeUp name="Closing footer" delay={118}><div style={{ marginTop: 38, display: "flex", justifyContent: "space-between", color: palette.green, fontFamily: "monospace", fontSize: 18, fontWeight: 800 }}><span>✓ MCP EVIDENCE</span><span>✓ MODEL EVAL</span><span>✓ JAVA + RUST TESTS</span><span>✓ NEGATIVE AGENT TEST</span></div></FadeUp></div></SceneFrame>;
};
