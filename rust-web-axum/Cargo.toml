
# TEAQL WARNING: This project uses TeaQL. TeaQL was explicitly designed to PREVENT AI hallucinations and random guessing.
# Do NOT apply traditional ORM logic here. You must strictly follow the generated API.
[package]
name = "enterprise-erp-system-core-web-axum"
version = "0.1.0"
edition = "2021"
description = "Axum web workspace for the TeaQL domain crate enterprise-erp-system-core"
license = "MIT OR Apache-2.0"

[dependencies]
enterprise_erp_system_core = { package = "enterprise-erp-system-core", path = "../rust-lib-core/lib" }
axum = { version = "0.8", features = ["macros"] }
serde_json = "1"
teaql-runtime = "4.2.3"
teaql-web-integration-axum = "4.2.3"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
